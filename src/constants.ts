export const WHEEL_ZOOM_STEP = 0.005;
export const MIN_ZOOM = 0;
export const MAX_ZOOM = 10;
export const SCENE_SIZE = 20000;

/** Margin to scene size ratio for initial zoom */
export const INITIAL_SCENE_MARGIN_RATIO = 0.05;

/** Rotation field minimum value */
export const ROTATION_FIELD_MIN_VALUE = -Math.PI;

/** Rotation field maximum value */
export const ROTATION_FIELD_MAX_VALUE = Math.PI;

/** Rotation field value step */
export const ROTATION_FIELD_VALUE_STEP = 0.01;
